import { Open_Sans } from 'next/font/google';

export const PRIMARY_FONT = Open_Sans({
    subsets: ['latin'],
    weight: ['400', '500', '600', '700'],
    display: 'swap',
    adjustFontFallback: false,
});
